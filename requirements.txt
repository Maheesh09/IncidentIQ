# Web framework
fastapi==0.115.6                          # was 0.115.0 — unlocks starlette 0.40 SSRF fix
uvicorn[standard]==0.30.6
prometheus-fastapi-instrumentator==7.0.0
starlette==0.40.0                         # ADD — pins the SSRF/DoS fix explicitly

# LangGraph + LangChain + Gemini
langgraph==0.2.28
langchain==0.3.27                         # was 0.3.1 — required for patched core + langsmith
langchain-core==0.3.85                    # ADD — fixes CRITICAL RCE CVE-2025-68664
langchain-text-splitters==0.3.9           # ADD — fixes XXE CVE-2025-6985
langsmith==0.3.45                         # ADD — fixes file-read + deserialization CVEs
langchain-google-genai==2.0.1

# Utilities
python-multipart==0.0.18                  # was 0.0.12 — fixes path-traversal + DoS